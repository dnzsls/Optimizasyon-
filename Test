"""
KOMPLE KAPASİTE PLANLAMA - V2
Vardiyalar Excel'den okunur, saat bazlı kesişim
"""
import pandas as pd
import math

# ============= KONFIGURASYON =============

CONFIG = {
    'aht': 180,              # Average Handle Time - saniye
    'target_asa': 30,        # Hedef yanıt süresi - saniye
    'shrinkage': 0.10,       # Mola/izin payı - %10
    'interval_minutes': 15   # İnterval - dakika
}


# ============================================================================
# VARDİYA OKUMA (EXCEL'DEN)
# ============================================================================

def load_shifts_from_excel(excel_path):
    """
    Excel'den vardiya tanımlarını oku
    
    Excel formatı:
    - shift: Vardiya adı (V1, V2, ...)
    - start: Başlangıç saati ('07:00')
    - end: Bitiş saati ('16:00')
    - company: (Opsiyonel) A21 veya B21
    
    Örnek:
    shift  | start | end   | company
    V1     | 07:00 | 16:00 | A21
    V2     | 08:00 | 17:00 | A21
    """
    df = pd.read_excel(excel_path)
    
    # Gerekli kolonlar
    required = ['shift', 'start', 'end']
    missing = [col for col in required if col not in df.columns]
    if missing:
        raise ValueError(f"Eksik kolonlar: {missing}")
    
    shifts = []
    for _, row in df.iterrows():
        shift_dict = {
            'name': str(row['shift']),
            'start': str(row['start']) if not hasattr(row['start'], 'strftime') else row['start'].strftime('%H:%M'),
            'end': str(row['end']) if not hasattr(row['end'], 'strftime') else row['end'].strftime('%H:%M')
        }
        
        # Opsiyonel: company kolonu
        if 'company' in df.columns:
            shift_dict['company'] = str(row['company'])
        
        shifts.append(shift_dict)
    
    print(f"✅ {len(shifts)} vardiya yüklendi")
    return shifts


def create_default_shifts():
    """Varsayılan 19 vardiya (Excel yoksa kullan)"""
    shifts = [
        # A21 Vardiyaları
        {'name': 'V1', 'start': '07:00', 'end': '16:00', 'company': 'A21'},
        {'name': 'V2', 'start': '08:00', 'end': '17:00', 'company': 'A21'},
        {'name': 'V3', 'start': '09:00', 'end': '18:00', 'company': 'A21'},
        {'name': 'V4', 'start': '10:00', 'end': '19:00', 'company': 'A21'},
        {'name': 'V5', 'start': '15:00', 'end': '24:00', 'company': 'A21'},
        {'name': 'V6', 'start': '17:00', 'end': '01:00', 'company': 'A21'},
        {'name': 'V7', 'start': '18:00', 'end': '02:00', 'company': 'A21'},
        {'name': 'V8', 'start': '19:00', 'end': '03:00', 'company': 'A21'},
        {'name': 'V9', 'start': '00:00', 'end': '08:00', 'company': 'A21'},
        # B21 Vardiyaları
        {'name': 'V10', 'start': '08:00', 'end': '17:30', 'company': 'B21'},
        {'name': 'V11', 'start': '09:00', 'end': '18:30', 'company': 'B21'},
        {'name': 'V12', 'start': '10:00', 'end': '19:30', 'company': 'B21'},
        {'name': 'V13', 'start': '11:00', 'end': '20:30', 'company': 'B21'},
        {'name': 'V14', 'start': '12:00', 'end': '21:30', 'company': 'B21'},
        {'name': 'V15', 'start': '13:00', 'end': '22:30', 'company': 'B21'},
        {'name': 'V16', 'start': '14:00', 'end': '23:30', 'company': 'B21'},
        {'name': 'V17', 'start': '15:00', 'end': '00:30', 'company': 'B21'},
        {'name': 'V18', 'start': '17:00', 'end': '01:30', 'company': 'B21'},
        {'name': 'V19', 'start': '18:00', 'end': '02:30', 'company': 'B21'},
    ]
    return shifts


# ============================================================================
# BÖLÜM 1: INTERVAL BAZLI AGENT İHTİYACI (ERLANG C)
# ============================================================================

def erlang_c(agents, traffic):
    if agents <= traffic or agents == 0:
        return 1.0
    erlang_b = traffic / agents
    for i in range(2, int(agents) + 1):
        erlang_b = (traffic * erlang_b) / (i + traffic * erlang_b)
    erlang_c_val = erlang_b / (1 - (traffic / agents) * (1 - erlang_b))
    return min(erlang_c_val, 1.0)


def calculate_asa(agents, traffic, aht):
    if agents <= traffic:
        return 999
    ec = erlang_c(agents, traffic)
    asa = (ec * (aht / 60)) / (agents - traffic) * 60
    return asa


def find_agents_for_target(calls, interval_min, aht, target_asa):
    if calls == 0:
        return 0
    traffic = (calls * (aht / 60)) / interval_min
    if traffic == 0:
        return 0
    min_agents = math.ceil(traffic)
    max_agents = math.ceil(traffic * 2) + 5
    for agents in range(min_agents, max_agents + 1):
        asa = calculate_asa(agents, traffic, aht)
        if asa <= target_asa:
            return agents
    return max_agents


def calculate_interval_requirements(df):
    """Her interval için agent ihtiyacını hesapla"""
    results = []
    
    for idx, row in df.iterrows():
        date = row['date']
        time = row['time']
        
        # Time object ise string'e çevir
        if hasattr(time, 'strftime'):
            time = time.strftime('%H:%M')
        elif not isinstance(time, str):
            time = str(time)
        
        calls_a = row['queue_a']
        calls_b = row['queue_b']
        
        # A için
        agents_a_raw = find_agents_for_target(
            calls_a, CONFIG['interval_minutes'], 
            CONFIG['aht'], CONFIG['target_asa']
        )
        agents_a_final = math.ceil(agents_a_raw / (1 - CONFIG['shrinkage']))
        
        # B için
        agents_b_raw = find_agents_for_target(
            calls_b, CONFIG['interval_minutes'],
            CONFIG['aht'], CONFIG['target_asa']
        )
        agents_b_final = math.ceil(agents_b_raw / (1 - CONFIG['shrinkage']))
        
        results.append({
            'date': date,
            'time': time,
            'calls_a': calls_a,
            'calls_b': calls_b,
            'agents_a': agents_a_final,
            'agents_b': agents_b_final,
            'total_agents': agents_a_final + agents_b_final
        })
    
    return pd.DataFrame(results)


# ============================================================================
# BÖLÜM 2: VARDİYALARA SAAT BAZLI DAĞITIM (YENİ)
# ============================================================================

def time_to_minutes(time_str):
    if hasattr(time_str, 'strftime'):
        time_str = time_str.strftime('%H:%M')
    h, m = map(int, str(time_str).split(':'))
    return h * 60 + m


def is_time_in_shift(time_str, shift_start, shift_end):
    time_min = time_to_minutes(time_str)
    start_min = time_to_minutes(shift_start)
    end_min = time_to_minutes(shift_end)
    
    if end_min <= start_min:  # Gece vardiyası
        return time_min >= start_min or time_min < end_min
    else:
        return start_min <= time_min < end_min


def distribute_by_hour(interval_df, shifts):
    """
    SAAT BAZLI DAĞITIM (YENİ YAKLAŞIM)
    
    Her interval için:
    1. O saatte kaç vardiya çalışıyor say
    2. İhtiyacı eşit böl
    3. Her vardiyaya payını ver
    """
    results = []
    
    for idx, row in interval_df.iterrows():
        date = row['date']
        time = row['time']
        total_needed = row['total_agents']
        
        if total_needed == 0:
            continue
        
        # Bu saatte çalışan vardiyaları bul
        working_shifts = []
        for shift in shifts:
            if is_time_in_shift(time, shift['start'], shift['end']):
                working_shifts.append(shift['name'])
        
        if len(working_shifts) == 0:
            continue
        
        # EŞİT DAĞIT (saat bazlı)
        agents_per_shift = total_needed / len(working_shifts)
        
        for shift_name in working_shifts:
            results.append({
                'date': date,
                'time': time,
                'shift': shift_name,
                'agents_needed': agents_per_shift,
                'shifts_working': len(working_shifts)
            })
    
    return pd.DataFrame(results)


def aggregate_shifts(distribution_df, shifts):
    """Her vardiya için MAX agent sayısı"""
    shift_results = []
    
    for date in sorted(distribution_df['date'].unique()):
        date_data = distribution_df[distribution_df['date'] == date]
        
        for shift in shifts:
            shift_data = date_data[date_data['shift'] == shift['name']]
            
            if len(shift_data) > 0:
                max_agents = math.ceil(shift_data['agents_needed'].max())
            else:
                max_agents = 0
            
            if max_agents > 0:
                shift_results.append({
                    'date': date,
                    'shift': shift['name'],
                    'start': shift['start'],
                    'end': shift['end'],
                    'agents': max_agents
                })
    
    return pd.DataFrame(shift_results)


def verify_coverage(interval_df, vardiya_df, shifts):
    """Doğrulama"""
    verify_results = []
    
    for idx, row in interval_df.iterrows():
        date = row['date']
        time = row['time']
        needed = row['total_agents']
        
        if needed == 0:
            continue
        
        date_shifts = vardiya_df[vardiya_df['date'] == date]
        
        total_available = 0
        for _, shift_row in date_shifts.iterrows():
            shift_info = next((s for s in shifts if s['name'] == shift_row['shift']), None)
            if shift_info and is_time_in_shift(time, shift_info['start'], shift_info['end']):
                total_available += shift_row['agents']
        
        verify_results.append({
            'date': date,
            'time': time,
            'needed': needed,
            'available': total_available,
            'sufficient': total_available >= needed,
            'diff': total_available - needed
        })
    
    return pd.DataFrame(verify_results)


# ============================================================================
# ANA FONKSİYON
# ============================================================================

def calculate_complete_planning(df, shifts=None, shift_excel=None):
    """
    Komple kapasite planlaması
    
    Parameters:
    - df: Çağrı tahmini (date, time, queue_a, queue_b)
    - shifts: Vardiya listesi (dict) VEYA
    - shift_excel: Vardiya Excel dosyası yolu
    """
    
    print("=" * 70)
    print("KOMPLE KAPASİTE PLANLAMA - V2")
    print("=" * 70)
    
    # Vardiyaları yükle
    if shift_excel:
        print(f"\n📂 Vardiyalar Excel'den yükleniyor: {shift_excel}")
        shifts = load_shifts_from_excel(shift_excel)
    elif shifts is None:
        print("\n📂 Varsayılan 19 vardiya kullanılıyor")
        shifts = create_default_shifts()
    else:
        print(f"\n📂 {len(shifts)} vardiya kullanılıyor")
    
    # BÖLÜM 1: Interval bazlı ihtiyaç
    print("\n⚙️ BÖLÜM 1: Interval bazlı agent ihtiyacı hesaplanıyor...")
    interval_df = calculate_interval_requirements(df)
    print(f"✅ {len(interval_df)} interval hesaplandı")
    print(f"   Peak: {interval_df['total_agents'].max()} agent")
    
    # BÖLÜM 2: Vardiyalara dağıt (SAAT BAZLI - YENİ)
    print("\n⚙️ BÖLÜM 2: Vardiyalara SAAT BAZLI dağıtım yapılıyor...")
    distribution_df = distribute_by_hour(interval_df, shifts)
    
    print("\n⚙️ BÖLÜM 3: Vardiya bazlı toplama yapılıyor...")
    vardiya_df = aggregate_shifts(distribution_df, shifts)
    print(f"✅ {len(vardiya_df)} vardiya-gün kombinasyonu")
    
    # BÖLÜM 3: Doğrulama
    print("\n⚙️ BÖLÜM 4: Doğrulama yapılıyor...")
    verify_df = verify_coverage(interval_df, vardiya_df, shifts)
    
    sufficient_count = verify_df['sufficient'].sum()
    total_count = len(verify_df)
    
    print(f"✅ {sufficient_count} / {total_count} interval yeterli")
    
    insufficient = verify_df[~verify_df['sufficient']]
    if len(insufficient) > 0:
        print(f"⚠️ {len(insufficient)} interval YETERSİZ!")
        print("\nİlk 5 yetersiz interval:")
        print(insufficient[['date', 'time', 'needed', 'available', 'diff']].head(5))
    
    print("\n" + "=" * 70)
    print("✅ TAMAMLANDI!")
    print("=" * 70)
    
    return interval_df, vardiya_df, verify_df


# ============================================================================
# KULLANIM
# ============================================================================

# ============================================================================
# KULLANIM ve TEST
# ============================================================================

def load_call_forecast_from_sql(connection_string, query=None):
    """
    SQL'den çağrı tahmini oku
    
    Parameters:
    - connection_string: SQL bağlantı string'i
    - query: SQL sorgusu (opsiyonel, varsayılan sorgu var)
    
    Örnek connection_string:
    - "mssql+pyodbc://user:pass@server/database?driver=ODBC+Driver+17+for+SQL+Server"
    - "postgresql://user:pass@localhost/database"
    """
    from sqlalchemy import create_engine
    
    if query is None:
        query = """
        SELECT 
            date,
            time,
            queue_a,
            queue_b
        FROM call_forecast
        WHERE date >= DATEADD(day, 0, GETDATE())
        ORDER BY date, time
        """
    
    engine = create_engine(connection_string)
    df = pd.read_sql(query, engine)
    
    print(f"✅ SQL'den {len(df)} satır veri okundu")
    return df


def create_sample_data():
    """Örnek test verisi oluştur"""
    dates = pd.date_range('2025-01-15', '2025-01-16', freq='D')
    times = pd.date_range('00:00', '23:45', freq='15min').strftime('%H:%M')
    
    rows = []
    for date in dates:
        for i, time in enumerate(times):
            # Gün içi pattern
            hour = int(time.split(':')[0])
            
            if 7 <= hour < 12:  # Sabah
                calls_a = 30 + (hour - 7) * 5
                calls_b = 15 + (hour - 7) * 3
            elif 12 <= hour < 18:  # Öğle-Akşam (PEAK)
                calls_a = 50 + (i % 4) * 5
                calls_b = 25 + (i % 4) * 3
            elif 18 <= hour < 24:  # Akşam
                calls_a = 40 - (hour - 18) * 3
                calls_b = 20 - (hour - 18) * 2
            else:  # Gece
                calls_a = 10
                calls_b = 5
            
            rows.append({
                'date': date.strftime('%Y-%m-%d'),
                'time': time,
                'queue_a': calls_a,
                'queue_b': calls_b
            })
    
    return pd.DataFrame(rows)


if __name__ == '__main__':
    
    print("""
╔══════════════════════════════════════════════════════════════════╗
║  KOMPLE KAPASİTE PLANLAMA - V2 (SAAT BAZLI DAĞITIM)             ║
╚══════════════════════════════════════════════════════════════════╝

KULLANIM:

# YÖNTEM 1: SQL'den oku
from sqlalchemy import create_engine

connection_string = "mssql+pyodbc://user:pass@server/db?driver=ODBC+Driver+17"
df = load_call_forecast_from_sql(connection_string)

interval_df, vardiya_df, verify_df = calculate_complete_planning(
    df, 
    shift_excel='vardiyalar.xlsx'
)

# YÖNTEM 2: Excel'den oku (eski yöntem)
df = pd.read_excel('cagri_tahminleri.xlsx')
interval_df, vardiya_df, verify_df = calculate_complete_planning(df)

═══════════════════════════════════════════════════════════════════
""")
    
    print("\n" + "=" * 70)
    print("ÖRNEK VERİ İLE TEST")
    print("=" * 70)
    
    # Örnek veri oluştur
    print("\n📊 Örnek 2 günlük veri oluşturuluyor...")
    df = create_sample_data()
    print(f"✅ {len(df)} satır veri hazır")
    print(f"   Tarih aralığı: {df['date'].min()} - {df['date'].max()}")
    print(f"   Toplam A çağrısı: {df['queue_a'].sum()}")
    print(f"   Toplam B çağrısı: {df['queue_b'].sum()}")
    
    # İlk 10 satırı göster
    print("\nİlk 10 satır:")
    print(df.head(10).to_string(index=False))
    
    # Hesapla
    print("\n" + "=" * 70)
    interval_df, vardiya_df, verify_df = calculate_complete_planning(df)
    
    # Sonuçları göster
    print("\n" + "=" * 70)
    print("📊 SONUÇLAR")
    print("=" * 70)
    
    print("\n1️⃣ INTERVAL SONUÇLARI (İlk 10):")
    print(interval_df[['date', 'time', 'calls_a', 'calls_b', 'agents_a', 'agents_b', 'total_agents']].head(10).to_string(index=False))
    
    print("\n2️⃣ VARDİYA PLANI (İlk 10):")
    print(vardiya_df[['date', 'shift', 'start', 'end', 'agents']].head(10).to_string(index=False))
    
    print("\n3️⃣ DOĞRULAMA (İlk 10):")
    print(verify_df[['date', 'time', 'needed', 'available', 'sufficient', 'diff']].head(10).to_string(index=False))
    
    # Özet
    print("\n" + "=" * 70)
    print("📈 ÖZET")
    print("=" * 70)
    print(f"Toplam interval: {len(interval_df)}")
    print(f"Peak agent ihtiyacı: {interval_df['total_agents'].max()}")
    print(f"Toplam vardiya-gün: {len(vardiya_df)}")
    print(f"Yeterli interval: {verify_df['sufficient'].sum()} / {len(verify_df)}")
    
    insufficient = verify_df[~verify_df['sufficient']]
    if len(insufficient) > 0:
        print(f"\n⚠️ YETERSİZ İNTERVALLER: {len(insufficient)}")
        print(insufficient[['date', 'time', 'needed', 'available', 'diff']].head(10).to_string(index=False))
    else:
        print("\n✅ TÜM İNTERVALLER YETERLİ!")
    
    print("\n" + "=" * 70)
    print("✅ TEST TAMAMLANDI!")
    print("=" * 70)
