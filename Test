"""
FONKSİYON 1: Traffic Hesaplama
En temel fonksiyon - Traffic nedir öğrenelim
"""

def calculate_traffic(calls, interval_min, aht):
    """
    Traffic (Erlang): Çağrı yoğunluğunu hesaplar
    
    Formül:
    Traffic = (Çağrı_Sayısı × AHT_dakika) / İnterval_dakika
    
    Ne demek?
    - Traffic = Sürekli kaç agent meşgul kalır?
    """
    traffic = (calls * (aht / 60)) / interval_min
    return traffic


# TEST EDELİM
if __name__ == '__main__':
    print("=" * 60)
    print("FONKSİYON 1: TRAFFIC HESAPLAMA")
    print("=" * 60)
    
    # Örnek 1: Az yoğun
    calls1 = 20
    traffic1 = calculate_traffic(calls1, interval_min=30, aht=180)
    print(f"\nÖrnek 1: Az Yoğun")
    print(f"  Çağrı: {calls1} adet")
    print(f"  Interval: 30 dakika")
    print(f"  AHT: 180 saniye (3 dakika)")
    print(f"  Traffic: {traffic1:.2f} Erlang")
    print(f"  Yorum: {traffic1:.0f} agent sürekli meşgul kalır")
    
    # Örnek 2: Orta yoğun
    calls2 = 50
    traffic2 = calculate_traffic(calls2, interval_min=30, aht=180)
    print(f"\nÖrnek 2: Orta Yoğun")
    print(f"  Çağrı: {calls2} adet")
    print(f"  Traffic: {traffic2:.2f} Erlang")
    print(f"  Yorum: {traffic2:.0f} agent sürekli meşgul kalır")
    
    # Örnek 3: Çok yoğun
    calls3 = 100
    traffic3 = calculate_traffic(calls3, interval_min=30, aht=180)
    print(f"\nÖrnek 3: Çok Yoğun")
    print(f"  Çağrı: {calls3} adet")
    print(f"  Traffic: {traffic3:.2f} Erlang")
    print(f"  Yorum: {traffic3:.0f} agent sürekli meşgul kalır")
    
    print("\n" + "=" * 60)
    print("💡 ÖNEMLI:")
    print("  Traffic = Minimum gereken agent sayısı")
    print("  Ama hedef ASA için daha fazla agent gerek!")
    print("=" * 60)
